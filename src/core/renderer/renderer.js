export default class Renderer {

  constructor () {
    this._backgroundColor = '#000'
    this._width = 200
    this._height = 200
    this._pixelRatio = 2
  }

  render (canvas) {}

  resize (width, height) {}
}
